---
# Positive fixture for kube-worker/main: declared-option projection of the
# furyctl-rendered outdir values (real). MUST pass validate_argument_spec.
kubectl_bin: /usr/local/bin/kubectl
kubernetes_kubeconfig_path: ./
kubernetes_version: 1.35.5
upgrade: true
kubernetes_control_plane_address: api.example.local:6443
