---
# Negative fixture for kube-control-plane/main: omits required 'kubernetes_version'.
# MUST fail validate_argument_spec (asserted via block/rescue).
kubelet_csr_approver_tag: v1.0
kubernetes_kubeconfig_path: ./
kubernetes_local_pki_dir: /etc/kubernetes/pki
upgrade: true
