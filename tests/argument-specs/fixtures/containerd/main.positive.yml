---
# Positive fixture for containerd/main: the variables a real furyctl run provides. MUST pass validate_argument_spec.
containerd_sandbox_tag: 3.10.1
kubernetes_image_registry: registry.example.io/fury
upgrade: true
