---
# Negative fixture for kube-worker/main: omits the required kubernetes_version.
# MUST fail validate_argument_spec.
kubectl_bin: /usr/local/bin/kubectl
kubernetes_kubeconfig_path: ./
upgrade: true
