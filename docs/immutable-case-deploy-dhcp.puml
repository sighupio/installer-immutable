@startuml immutable-case-deploy-dhcp
!theme cyborg

title Case 3 - Deploy a new DHCP + PXE

start

:Deploy a new DHCP server with HTTP-boot flags\n(UEFI HTTP Boot -> ipxe.efi, then furyctl);

:Node powered on -> network-boots\noff the new DHCP server;

:furyctl serves the iPXE config\nbased on the node's MAC address;

:Continue to the shared flow;

stop

@enduml
