---
# Positive fixture for upgrade-gates/cluster_health_gate: the variables a real furyctl run provides. MUST pass validate_argument_spec.
kubectl_bin: /usr/local/bin/kubectl
kubernetes_kubeconfig_path: ./
kubernetes_version: 1.35.5
